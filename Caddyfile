:81 {
	# Socket.IO do game server. Caddy encaminha HTTP polling e WebSocket
	# automaticamente para o serviço Bun na porta 3003.
	handle /socket.io/* {
		reverse_proxy 127.0.0.1:3003
	}

	# Todo o restante pertence ao frontend Next.js.
	handle {
		reverse_proxy 127.0.0.1:3000
	}
}
